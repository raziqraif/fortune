export enum Type {
    // auth types
    LOGIN,
    LOGIN_FAILED,
    LOGIN_SUCCEEDED,
    REGISTER,
    REGISTER_FAILED,
    REGISTER_SUCCEEDED,
    LOGOUT,
    VERIFY_AUTH_TOKEN,
    VERIFY_AUTH_TOKEN_SUCCEEDED,
    SET_VERIFY_FAILED,

    // coin types
    SET_COINS,

    // game types
    CREATE_GAME,
    CREATE_GAME_FAILED,
    GET_ACTIVE_GAMES,
    GET_ACTIVE_GAMES_FAILED,
    JOIN_GAME,
    JOIN_GAME_FAILED,
    SET_GAME,
    SET_GAME_FAILED,

    SET_REGISTRATION_ERROR,
    SET_LOGIN_ERROR,
}
