import React from 'react';
import {render, fireEvent, screen} from '@testing-library/react';

import CoinInfo from './CoinGraph';


test('TODO, () => {
  const {getByText} = render(<TODO>)
  expect(getByText('TODO')).toBeInTheDocument() //TODO
})
